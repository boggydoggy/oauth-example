spring:
  application:
    name: 'OAuth'
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ''
            client-secret: '''
            scope: profile,email